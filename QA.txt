Q1 — Payment scope: one ticket vs. cart/order?
Your ticket lifecycle is Available → Reserved → Confirmed. Payments sit between Reserve and Confirm. Question: should one Stripe Checkout Session represent exactly one ticket, or can a session cover multiple tickets (cart)?
# Questions
Should one Stripe Checkout Session cover exactly one ticket, or multiple tickets (cart/order)?
One ticket per session (Recommended)

Q2 — Checkout creation precondition: must the ticket already be reserved?
Your Reserve endpoint puts a ticket in Reserved (assigns UserId). Should POST /checkout require the ticket to already be reserved by the calling user (fail otherwise), or should checkout itself create the reservation?
# Questions
When POST /checkout is called, must the ticket already be reserved by the calling user?
Require prior reservation (Recommended)

Q3 — Identity source for checkout/verify: request body or JWT claims?
Current controllers (TicketsController) take UserId in the request body even though endpoints are [Authorize]. For payments, where should the acting user come from?
# Questions
For checkout and verify endpoints, should the acting user id come from the JWT claims or the request body?
From JWT claims (Recommended)

Q4 — Domain model shape: Payment aggregate + PaymentStatus enum?
Proposed: new Payment aggregate root with PaymentId (Guid), owning the Stripe Checkout SessionId, TicketId, UserId, Money Amount, PaymentStatus, timestamps. Statuses: Pending (session created) → Completed (verified paid) / Failed / Expired. Confirm this shape, and whether Payment stores a snapshot of the amount/currency at creation (vs. reading live from the ticket).
# Questions
How should the Payment aggregate be shaped?
Snapshot Payment + 4 statuses (Recommended)

Q5 — Stripe integration: official Stripe.NET SDK or raw HTTP?
The repo's only outbound integration is MailKit. For Stripe, the standard approach is the official Stripe.net package (SessionService.CreateAsync, SessionService.GetAsync).
# Questions
Should we use the official Stripe.net SDK or a raw REST client for Stripe calls?
Stripe.net SDK (Recommended)

Q6 — Verify mechanism: on-demand Stripe retrieval vs. webhook?
You said two endpoints (no webhook). Option A: POST /verify calls SessionService.GetAsync(sessionId), checks PaymentStatus == "paid", then confirms the ticket and marks Payment Completed. Option B: additionally set up a Stripe webhook listener (the production-standard approach — Stripe pushes the event to you, no polling). Option B is strictly more robust but adds a third endpoint/infrastructure.
# Questions
How should payment success be determined?
My intention was to one of this endpoints actually be a webhook

Ah, that changes things — one of the two endpoints is the Stripe webhook. So the two endpoints are:
1. Create checkout — user-facing, generates a Stripe Checkout Session.
2. Webhook — Stripe calls us with checkout.session.completed; that's how success is detected. The "verify" isn't a client call at all.
Q7 — Confirm the endpoint map. Endpoint 2 is POST /api/payments/webhook receiving checkout.session.completed events, verifying the Stripe signature, then marking the Payment Completed and confirming the ticket. There is no client-facing "verify" endpoint — the webhook is the verify. Correct?
# Questions
Is this the intended design?
Yes, webhook = verify (Recommended)

Q8 — Webhook endpoint security. Stripe can't send a JWT, so the webhook endpoint must be [AllowAnonymous] and rely purely on Stripe signature verification (EventUtility.ConstructEvent(body, sigHeader, webhookSecret)), returning 400 on invalid signature. Confirm we accept this (unauthenticated-but-signature-verified) model for the webhook?
# Questions
How should the webhook endpoint be secured?
AllowAnonymous + signature check (Recommended)

Q9 — Webhook processing: CQRS command or direct service?
You said requests should be handled "same manner as tickets" (CQRS + Result). Should the webhook handler also be a Command/Handler pair (e.g., StripeWebhookCommand → StripeWebhookHandler dispatched from the controller), or a direct service call since Stripe doesn't care about HTTP response bodies?
# Questions
Should the webhook flow be implemented as a CQRS command handler?
CQRS command (Recommended)

Q10 — Idempotency + duplicates. Two threats: (a) Stripe redelivers the same webhook event, (b) user calls checkout twice for the same ticket, creating two sessions. How to handle?
# Questions
How should idempotency and duplicate checkouts be handled?
No-op on re-delivery + block duplicate checkout (Recommended)

Q11 — Non-success paths: expiry & failure. If the user abandons checkout, the session expires and the Payment stays Pending forever. Do you want (a) a background job (like the existing ExpiredReservationsCleanupJob) to mark stale Pending payments Expired and release the ticket reservation, or (b) handle Expired/Failed only as an offline concern (e.g., DB patch) for now? Also, does Failed need a real trigger, or do we skip it?
# Questions
How should expired/failed payments be handled?
Full Failed handling too

Q12 — Ticket fate on Failed/Expired. When a payment fails (or the expiry job marks it Expired), the ticket is still Reserved and blocks other buyers. Release it (back to Available via ReleaseReservation, which already exists and raises TicketReleasedEvent)?
# Questions
What happens to the reserved ticket when payment fails or expires?
Release reservation (Recommended)

Q13 — Webhook event coverage. With Full Failed handling, the webhook must branch on event type. Which set should we support?
# Questions
Which Stripe event types should the webhook handle?
Completed + 2 failed types (Recommended)

Q14 — Matching the webhook event to a Payment. When creating the Checkout Session, we'll set Stripe's client_reference_id to the PaymentId (Stripe's recommended way to link sessions back to your records). On webhook, look up Payment by client_reference_id from the event's data.object. Confirm we use client_reference_id = PaymentId (vs. storing sessionId on Payment and matching that)?
# Questions
How should the webhook event map back to the stored Payment?
client_reference_id = PaymentId (Recommended)

Q15 — Money ↔ Stripe minor units. Stripe requires integer amounts in minor units (grosze for PLN). Money.Amount is decimal (150.00 PLN). Where should the decimal → long conversion live, and how strictly to round?
# Questions
Where should decimal → minor-units conversion live?
Convert in Stripe service (Recommended)

Q16 — Stripe service abstraction. Following the IEmailSender/MimeKitEmailSender precedent (interface in Infrastructure, impl in Infrastructure), should we create an IStripePaymentService (Infrastructure/Services or Infrastructure/Payments folder) with methods like CreateCheckoutSessionAsync(...) and RetrieveSessionAsync(...), registered in DI, with a settings class StripeSettings (SecretKey, WebhookSecret, SuccessUrl, CancelUrl, Currency) in appsettings?
# Questions
How should the Stripe client be wrapped?
Use recommended but name abstraction as IPaymentService

Q17 — Checkout command flow + return payload. POST /api/payments/checkout (Authorize, user from JWT) → CreateCheckoutSessionCommand handler: validates ticket exists & is Reserved by caller & no active Payment → creates Payment (Pending) → calls IPaymentService.CreateCheckoutSessionAsync → stores client_reference_id = PaymentId, sessionId → saves. Returns { checkoutUrl, sessionId, paymentId }. Confirm flow and payload, and that success_url/cancel_url come from config (StripeSettings.SuccessUrl/CancelUrl)?
# Questions
Confirm the checkout command flow and return payload?
As described (Recommended)

Q18 — Webhook command + response. Webhook controller: [AllowAnonymous], [HttpPost("webhook")] reads raw body + Stripe-Signature header, calls EventUtility.ConstructEvent (400 on failure), then dispatches StripeWebhookCommand (carries the typed Stripe.Event). Handler branches by event type (checkout.session.completed → Confirm; failed types → release + mark Failed; unknown → no-op). Controller returns Ok() unless the command failed. Confirm?
# Questions
Confirm webhook command structure and HTTP response behavior?
As described (Recommended)

Q19 — Payment domain events. Existing aggregates raise domain events (TicketConfirmedEvent, etc.) dispatched in SaveChangesAsync. Should the Payment aggregate raise events too (e.g., PaymentCompletedEvent, PaymentFailedEvent), even if no handler consumes them yet?
# Questions
Should the Payment aggregate raise domain events?
Raise events (Recommended)

Q20 — Wiring ticket confirmation on Completed. When checkout.session.completed arrives: mark Payment Completed, then ticket.Confirm(userId) (existing method requires matching UserId). The Payment already holds UserId/TicketId, so the handler loads both via UnitOfWork and confirms. Confirm this wiring, including that Confirm also releases... actually Confirm already transitions Reserved→Confirmed and raises TicketConfirmedEvent. Confirm the handler calls it?
# Questions
Should the completed webhook also confirm the ticket?
Confirm ticket (Recommended)

Q21 — Test scope. Existing handler tests use a real ServiceProvider + InMemory DB + mocked dispatcher; domain entity tests assert on DomainEvents. For payments: unit-test Payment entity state transitions + domain events, handler tests (checkout creates Payment + calls service; completed webhook confirms; failed releases; idempotent re-delivery no-ops) mocking IPaymentService and the domain-event dispatcher. Confirm scope, and whether webhook signature validation itself needs a test?
# Questions
Confirm the test scope?
Entity + handler tests (Recommended)

Now let me think about remaining decisions:

1. Where does the expiry job live — extend existing ExpiredReservationsCleanupJob or a new ExpiredPaymentsCleanupJob? The existing job releases reservations. But now failed payments release reservations. The expiry job should mark stale Pending payments as Expired AND release ticket reservation. Actually the existing ExpiredReservationsCleanupJob probably releases tickets that were reserved too long (10 min). Hmm, if payment is created after reservation, the reservation job might release the ticket before payment completes! That's a conflict. Let me check the existing job.

Let me look at ExpiredReservationsCleanupJob to understand.

Actually, let me read that file and the scratch specs.
Let me check the existing cleanup job and the specs before finalizing — there's a potential conflict between the reservation expiry job and the new payment flow.
→Read TicketReservationSystem\Infrastructure\Jobs\ExpiredReservationsCleanupJob.cs 
✱Glob ".scratch/**/*.md" 

The existing job releases reservations after 10 minutes. This creates a conflict: if a user reserves a ticket and starts checkout, and doesn't complete within 10 minutes, the reservation is released but the payment may still be Pending. When the webhook arrives, the ticket is Available but the Payment was created for it.

Actually the payment flow: reserve → checkout (creates Payment Pending) → pay → webhook (mark Completed, Confirm ticket). If the reservation cleanup job releases the ticket before the user pays, then webhook arrives: the ticket is now Available (not Reserved). Confirm would fail (Confirm requires Reserved status and matching user). Hmm.

This is an important integration concern. Let me ask about it. Actually, wait — the existing job releases Reserved tickets after 10 minutes. With payments in play, should reserved tickets with an active Pending payment NOT be released by that job? Or should the payment flow be integrated into the cleanup logic?

Options:
1. The ExpiredReservationsCleanupJob should skip tickets that have an active Pending Payment.
2. The new ExpiredPaymentsCleanupJob marks stale Pending payments as Expired (after session expiry ~24h for Stripe checkout) and releases the ticket.

So there's interplay. Let me ask about this. This is Q22.
Important integration find: the existing ExpiredReservationsCleanupJob releases any Reserved ticket after 10 minutes. If a user reserves a ticket and takes longer to complete checkout, that job would release the ticket while a Pending payment exists — then the webhook arrives and Confirm fails because the ticket is back to Available. How to reconcile?
# Questions
How should reservation expiry interact with in-flight payments?
Skip pending-paid tickets + separate payment job (Recommended)

Q22 — Store Stripe sessionId on Payment? We agreed webhook matching uses client_reference_id = PaymentId. Should the Payment also persist the Stripe session id (audit/debug, and a fallback match key), or keep Payment free of Stripe session data entirely?
# Questions
Should the Payment entity store the Stripe session id?
Store sessionId too (Recommended)

Q23 — Query endpoint for payments? You asked for two endpoints (checkout + webhook). Should we also add a read query (e.g., GET /api/payments/{paymentId} or list user's payments) for frontend/audit, or keep strictly to the two endpoints for now?