# 03 — EventsController tests

**What to build:** Tests covering GetEventById and GetEvents so the catalog endpoints
map query outcomes to the correct HTTP status.

**Blocked by:** None — can start immediately

**Status:** done

- [x] GetEventById returns 200 with the event when the event exists
- [x] GetEventById returns 404 when the event does not exist
- [x] GetEvents returns 200 with the event list
- [x] Test names follow the MethodName_Scenario_ExpectedResult convention
