- Extract Error classes to separate files
- replace magic numbers in SendAuthenticationCodeHandler Handle method
- rename EmailVerificationCode to VerificationCode and EmailVerificationCodeRepository to VerificationCodeRepository
- add global error handling middleware
- refactor folder structure in Infrastructure layer: 
    - create Services folder 
        - Email, 
        - Jobs, 
        - InMemory
        - Payment 
    - Move Authentication folder to Application layer, 
    - Move DomainEventHandlers to application layer
- Add currency check in StripePaymentService, Money value object has currency property, it should be verified if Money currency is the same as specified in AppSettings
- MinorUnitDivision logic in StripePaymentService should be done according to the currency, because there are some currencies which does not need division into minor units (they don't have minor unit)
- Refactor Payment entity 
    - delete StripeSessionId property
    - remove SetStripeSessionId method
    - create PaymentProvider property storing enum value of PaymentProvider 
    - create string ExternalId property storign sessionId 
- StripeWebhookHandler always return success, I know that this return value is never used so it doesn't really matter what is the output but it looks confusing when reading the logic of this handler. Refactor to return Failed result when handler fails to handle request
- Create separate controller for webhooks, move webhook from PaymentController into WebhooksController, give it a name StripeWebhook and route webhooks/stripe 
- remove ticket confirm endpoint from TicketController and related CQRS pipeline. From now on ticket confirmation will be done when payment will be registered


